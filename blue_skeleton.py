import bpy

# Create spheres and lines for blue skeleton in Blende rdtfyguhiojpk;l['r
# Sphere for nose
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(0.8, -7.6, 12.293113470077515)
)
bpy.context.object.name = 'nose'

# Sphere for left_eye_inner
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(1.0, -7.3, 13.042625188827515)
)
bpy.context.object.name = 'left_eye_inner'

# Sphere for left_eye
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(1.0, -7.3, 13.04993748664856)
)
bpy.context.object.name = 'left_eye'

# Sphere for left_eye_outer
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(1.0, -7.3, 13.060300350189209)
)
bpy.context.object.name = 'left_eye_outer'

# Sphere for right_eye_inner
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(0.0, -7.3, 12.956091165542603)
)
bpy.context.object.name = 'right_eye_inner'

# Sphere for right_eye
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(0.0, -7.3, 12.97215461730957)
)
bpy.context.object.name = 'right_eye'

# Sphere for right_eye_outer
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(0.0, -7.3, 12.998106479644775)
)
bpy.context.object.name = 'right_eye_outer'

# Sphere for left_ear
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(2.7, -4.8, 12.819727659225464)
)
bpy.context.object.name = 'left_ear'

# Sphere for right_ear
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(-1.8, -4.8, 12.567384243011475)
)
bpy.context.object.name = 'right_ear'

# Sphere for mouth_left
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(1.6, -6.7, 11.82368516921997)
)
bpy.context.object.name = 'mouth_left'

# Sphere for mouth_right
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(0.3, -6.7, 11.714283227920532)
)
bpy.context.object.name = 'mouth_right'

# Sphere for left_shoulder
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(6.1, -2.9, 9.123632311820984)
)
bpy.context.object.name = 'left_shoulder'

# Sphere for right_shoulder
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(-4.2, -3.3, 9.211033582687378)
)
bpy.context.object.name = 'right_shoulder'

# Sphere for left_elbow
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(6.2, -2.6, 4.385417401790619)
)
bpy.context.object.name = 'left_elbow'

# Sphere for right_elbow
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(-5.1, -2.5, 4.364664852619171)
)
bpy.context.object.name = 'right_elbow'

# Sphere for left_wrist
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(5.1, -5.0, -0.20157229155302048)
)
bpy.context.object.name = 'left_wrist'

# Sphere for right_wrist
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(-6.1, -4.7, -0.07925676181912422)
)
bpy.context.object.name = 'right_wrist'

# Sphere for left_pinky
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(4.6, -5.9, -2.0067189633846283)
)
bpy.context.object.name = 'left_pinky'

# Sphere for right_pinky
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(-6.7, -5.4, -1.6672493517398834)
)
bpy.context.object.name = 'right_pinky'

# Sphere for left_index
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(3.6, -6.6, -2.0722614228725433)
)
bpy.context.object.name = 'left_index'

# Sphere for right_index
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(-6.1, -6.2, -1.6866002976894379)
)
bpy.context.object.name = 'right_index'

# Sphere for left_thumb
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(4.5, -5.3, -0.603698305785656)
)
bpy.context.object.name = 'left_thumb'

# Sphere for right_thumb
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(-5.7, -5.1, -0.44591255486011505)
)
bpy.context.object.name = 'right_thumb'

# Sphere for left_hip
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(3.5, 0.0, -0.2053818665444851)
)
bpy.context.object.name = 'left_hip'

# Sphere for right_hip
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(-3.5, 0.0, 0.21952319890260696)
)
bpy.context.object.name = 'right_hip'

# Sphere for left_knee
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(2.0, -0.9, -7.773614525794983)
)
bpy.context.object.name = 'left_knee'

# Sphere for base_of_neck
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(0.9, -3.1, 9.167332947254181)
)
bpy.context.object.name = 'base_of_neck'

# Sphere for center_hip
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(0.0, 0.0, 0.007070666179060936)
)
bpy.context.object.name = 'center_hip'

# Sphere for center_of_head
bpy.ops.mesh.primitive_uv_sphere_add(
    radius=0.15,  # Adjust size of spheres
    location=(0.4, -4.8, 12.69355595111847)
)
bpy.context.object.name = 'center_of_head'

# Line connecting center_hip to base_of_neck
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (0.0, 0.0, 0.007070666179060936, 1)
spline.points[1].co = (0.9, -3.1, 9.167332947254181, 1)
curve_obj = bpy.data.objects.new(name='center_hip_to_base_of_neck', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

# Line connecting center_hip to left_hip
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (0.0, 0.0, 0.007070666179060936, 1)
spline.points[1].co = (3.5, 0.0, -0.2053818665444851, 1)
curve_obj = bpy.data.objects.new(name='center_hip_to_left_hip', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

# Line connecting center_hip to right_hip
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (0.0, 0.0, 0.007070666179060936, 1)
spline.points[1].co = (-3.5, 0.0, 0.21952319890260696, 1)
curve_obj = bpy.data.objects.new(name='center_hip_to_right_hip', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

# Line connecting base_of_neck to center_of_head
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (0.9, -3.1, 9.167332947254181, 1)
spline.points[1].co = (0.4, -4.8, 12.69355595111847, 1)
curve_obj = bpy.data.objects.new(name='base_of_neck_to_center_of_head', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

# Line connecting base_of_neck to left_shoulder
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (0.9, -3.1, 9.167332947254181, 1)
spline.points[1].co = (6.1, -2.9, 9.123632311820984, 1)
curve_obj = bpy.data.objects.new(name='base_of_neck_to_left_shoulder', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

# Line connecting base_of_neck to right_shoulder
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (0.9, -3.1, 9.167332947254181, 1)
spline.points[1].co = (-4.2, -3.3, 9.211033582687378, 1)
curve_obj = bpy.data.objects.new(name='base_of_neck_to_right_shoulder', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

# Line connecting left_shoulder to left_elbow
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (6.1, -2.9, 9.123632311820984, 1)
spline.points[1].co = (6.2, -2.6, 4.385417401790619, 1)
curve_obj = bpy.data.objects.new(name='left_shoulder_to_left_elbow', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

# Line connecting left_elbow to left_wrist
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (6.2, -2.6, 4.385417401790619, 1)
spline.points[1].co = (5.1, -5.0, -0.20157229155302048, 1)
curve_obj = bpy.data.objects.new(name='left_elbow_to_left_wrist', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

# Line connecting right_shoulder to right_elbow
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (-4.2, -3.3, 9.211033582687378, 1)
spline.points[1].co = (-5.1, -2.5, 4.364664852619171, 1)
curve_obj = bpy.data.objects.new(name='right_shoulder_to_right_elbow', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

# Line connecting right_elbow to right_wrist
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (-5.1, -2.5, 4.364664852619171, 1)
spline.points[1].co = (-6.1, -4.7, -0.07925676181912422, 1)
curve_obj = bpy.data.objects.new(name='right_elbow_to_right_wrist', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

# Line connecting left_hip to left_knee
curve = bpy.data.curves.new(type='CURVE', name='Line')
curve.dimensions = '3D'
spline = curve.splines.new(type='POLY')
spline.points.add(1)
spline.points[0].co = (3.5, 0.0, -0.2053818665444851, 1)
spline.points[1].co = (2.0, -0.9, -7.773614525794983, 1)
curve_obj = bpy.data.objects.new(name='left_hip_to_left_knee', object_data=curve)
bpy.context.scene.collection.objects.link(curve_obj)

